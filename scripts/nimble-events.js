// Event listeners and Foundry hooks for Nimble Action Tracker
// Exports: registerNimbleEvents

import { NimbleActionTracker } from './nimble-app.js';
import { canPlayerOpenTracker } from './nimble-utils.js';
import { TOOL_KEY, TOOL_LABEL, TOOL_ICON } from './nimble-constants.js';

export function registerNimbleEvents(trackerInstance) {
    // Move event listeners and hooks here
    // Example:
    // Hooks.on('updateUser', ...);
}

// Add more event/hook registration as needed
